# 账本改造清单与历史记录

## 待修改 / 待人工验收

- [ ] 在 Android 真机上验证轮班周期、弹性排班、离线启动和 `.xlsx` 导出。
- [ ] 在 iPhone/iPad Safari 或 Capacitor 客户端验证离线启动、文件分享和安全区适配。
- [ ] 根据实际单位规则确认自定义险种的缴费基数是否需要独立配置。
- [ ] 验收“导出 `.xlsx` → 本地修改 → 上传表格”的完整往返流程。
- [ ] 如需长期保存大量记录，再评估从 localStorage 迁移到 IndexedDB。
- [ ] 由项目维护者审阅代码，运行完整 Git 流程并决定是否提交。

## 已修改历史

### 多端 UI 与安全区域优化 · 本地待提交

- 小屏设置表单上下排列，统一下拉、日期、时间、金额输入的样式与触控高度。
- 险种编辑卡片重排，长文本和金额允许换行，日历与概览网格允许收缩。
- 页面顶部及横屏左右留出安全区；弹窗按动态可见视口限高并允许内部滚动。
- 保留浏览器缩放，增加键盘焦点提示和减少动画偏好支持。
- 更新离线缓存版本，同步 www 客户端资源。
- 待人工验收：iOS Safari/Android 浏览器工具栏收起展开、软键盘及真机横竖屏。

### 2026-09-05 · 本地待提交

- 增加医护/轮班排班：可配置周期起算日、连续上班天数和连续休息天数。
- 增加销售/弹性排班：默认不把周末视为休息日，按日历实际记录。
- 将五险一金改为可解耦配置：内置险种可单独启用/停用和调整比例。
- 增加自定义险种，可编辑名称、比例和启用状态。
- 工资明细改为动态显示已启用险种。
- 保留旧版设置字段，并为旧数据增加兼容迁移。
- 增加真正的 `.xlsx` 多工作表导出，同时保留 CSV 导出。
- 增加 `.xlsx/.csv` 表格上传入口，支持恢复考勤和储蓄记录。

### 2026-09-02

- 增加 PWA 安装引导和响应式安装卡片。
- 增加本地 CSV 导出、JSON 导入储蓄数据修复和 Service Worker 缓存版本更新。
- 初始化 Capacitor Android/iOS 客户端工程。
